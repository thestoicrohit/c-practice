#include<stdio.h>
int binary_search(int arr[],int n, int t)
{
    int st=0,end=n-1;
    while(st<=end)
    {
        int mid=(st+end)/2
        if(arr[mid]==k)
        {
            return mid;
        }
        else if(arr[mid]<k)
        {
            st=mid+1;
        }
    }
    else 
    {
        st=mid-1;
    }
}   
{
//find the smallest 
}